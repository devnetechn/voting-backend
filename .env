# Server
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# CORS (local + deployed frontend)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
JWT_SECRET=your-secret-key-here

# Postgres (LOCAL machine)
DB_USER=postgres
DB_PASS=123
DB_NAME=db_evoting
DB_HOST=127.0.0.1
DB_PORT=8000

# DATABASE_URL is left unset on purpose so the local DB_* vars above are used.
# When ready to point at Supabase, set DATABASE_URL to the Supabase connection string.
# Previous Render production URL (kept for reference, not active):
# DATABASE_URL=postgresql://db_evoting_user:I1OsSKseah4vr3WRtk6hqnOcVGWqFyW2@dpg-d394qjpr0fns738djcfg-a.oregon-postgres.render.com:5432/db_evoting
